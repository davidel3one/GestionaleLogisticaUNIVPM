# Stato del progetto e prossimi passi

Ultimo aggiornamento: 2026-07-12 (merge di `feat/rendicontazione-rf15-rf18` in `main`, dopo che PR #13 — RF14/RF19 — è già stata mergiata). Per lo stato git aggiornato in tempo reale, `git log`/`gh pr list` restano l'unica fonte autorevole — questa pagina è un'istantanea di orientamento, non sostituisce quello.

## Fatto

- Modello dati completo (`database/models.py`, `database/enums.py`), con tutte le entità del dominio, booleani con prefisso `flg_`.
- Import ordini da CSV (RF9), con validazione header, scarto righe malformate/ID duplicati (`logistica/gestore_logistica.py`), coperto da test.
- Modulo di autenticazione Admin (RNF5): registrazione, conferma email via OTP (bcrypt, scadenza 10 min, max 5 tentativi, cooldown 60s), login, sessioni (3 ore, token).
- CRUD generico (`database/crud_base.py`) + istanze minime per `Camion`/`Dipendente`/`Squadra`/`ComposizioneSquadra` (in `risorse/`) e per `EsitoConsegna`/`CausaleFallimento`/`RegistroEsiti`/`Allegato`/`ReportConsuntivo` (in `rendicontazione/`) — vedi `convenzioni-codice.md` per i limiti (nessuna logica di business sopra il CRUD, `delete()` è hard-delete non soft-delete).
- `FoglioViaggio` rimossa (vedi `divergenze-ea.md`).
- Bootstrap applicazione: creazione schema DB, logging su file, avvio finestra principale PySide6 (ancora vuota, per scelta — vedi `convenzioni-codice.md`, "backend prima, GUI dopo").
- **Geocoding offline (PR #9, mergiata il 2026-07-10):** `Ordine.destinazione` sostituito da `indirizzo`/`comune`/`provincia`/`lat`/`lon`; nuovo modulo `logistica/geocoding.py` + tabella statica `data/geocoding/comuni_coordinate.csv` (7897 comuni italiani). `GestoreLogistica.importa_ordini()` popola le coordinate durante l'import, con test per indirizzi malformati/comuni sconosciuti.
- **`Camion.peso_massimo`/`volume_massimo` (PR #10, mergiata il 2026-07-10):** capacità di peso/volume, prerequisito per i vincoli RF12/RF13.
- **RF12/RF13 (motore di ottimizzazione), PR #11 "Feat/ottimizzatore rf12 rf13", mergiata il 2026-07-11 (commit `7125476`):** `ottimizzazione/motore_ottimizzazione.py` con `suggerisci_ordini()` (RF12, knapsack 0/1 via PuLP/CBC) e `calcola_piano()`/`applica_piano()` (RF13, architettura a due fasi). RF13 usa clustering geografico (`ottimizzazione/clustering.py`, scikit-learn DBSCAN) → knapsack per cluster → vincolo di durata/orario di lavoro calcolato con un tour Held-Karp scritto a mano sotto una soglia di nodi calibrata empiricamente, fallback euristico nearest-neighbor sopra (`ottimizzazione/stima_durata.py`). RNF4 alzato da 30s a 3 minuti in questo passaggio. Storia completa (redesign da v1 cancellata per bug di calibrazione, stress test su 26 scenari, audit avversariale con 1 bug reale trovato e risolto) in `design-ottimizzatore.md`. Include anche 3 nuovi CSV di esempio con ordini sparsi su tutta Italia (`dati_esempio/Ordini_*_Nazionale.csv`), oltre ai 3 originali (solo Marche).
- **RF10/RF11 (composizione manuale viaggio + validazione), PR #12 "Implementazione della composizione manuale dei viaggi e validazione dei vincoli (RF10/RF11)", mergiata il 2026-07-11 (commit `a08541b`):** nuovo valore enum `StatoViaggio.IN_COMPOSIZIONE` per una bozza di viaggio persistita fin dal primo ordine. In `logistica/gestore_logistica.py`: `verifica_idoneita_risorsa()` (ex `MotoreOttimizzazione._ordine_idoneo`, spostata e resa pubblica, riusata da `ottimizzazione/` — stesso pattern di riuso cross-package già usato da `stima_durata.py`↔`geocoding.py`, niente package `common/`), `valida_ordine_per_viaggio()` (estende l'idoneità con capacità peso/volume residua e restituisce un motivo di rifiuto testuale), e tre operazioni per il ciclo di vita del viaggio manuale: `avvia_composizione_viaggio()` (imposta `data_partenza_prevista` già all'avvio, cruciale perché è il campo su cui `calcola_piano` verifica se una composizione è occupata quel giorno — evita il doppio-booking tra flusso manuale e automatico), `aggiungi_ordine_a_viaggio()` (valida contro lo stato accumulato, aggancia `viaggio_id`+`stato_ordine=PIANIFICATO` all'aggiunta non alla chiusura, per restare escluso dai candidati RF12/RF13), `chiudi_composizione_viaggio()` (richiede almeno un ordine). Estratta anche `crea_viaggio_persistito()` (riceve una `Session` aperta, non ne apre una propria) riusata sia dal flusso manuale sia da `MotoreOttimizzazione.applica_piano` (che non genera/persiste più i viaggi in proprio) — preserva l'atomicità della transazione batch esistente, verificata con un test dedicato che inietta un fallimento a metà batch. Pianificato con `task-exec` + pre-mortem (3 fix bloccanti trovati e risolti prima dell'implementazione; uno, il presunto CHECK constraint SQLite sul nuovo enum, si è rivelato una falsa premessa — `_enum_column` non genera CHECK su SQLite, verificato sul DDL reale). Storia completa in `design-ottimizzatore.md` e nella memoria personale `piano-rf10-rf11-v1.md`. Fuori scope in questo giro (segnalato esplicitamente, non implementato silenziosamente): rimozione di un ordine da una bozza, pulizia di bozze abbandonate, GUI.
- **RF14 (verifica partenza automatica) e RF19 (generazione report periodico), branch `feat/scheduler-rf14-rf19`, 2026-07-11:** nuovo modulo top-level `scheduler.py` (`BackgroundScheduler` di APScheduler, wired in `__init__.py main()`/`aboutToQuit`), con due job letti da `config.ini [scheduler]` (già presente, non modificato): `GestoreLogistica.verifica_partenze()` (RF14 — `IntervalTrigger`, porta i `Viaggio` `Pianificato` con partenza scaduta a `InCorso`, non tocca i viaggi ancora `IN_COMPOSIZIONE`) e `GestoreRendicontazione.genera_report_giornaliero()` (RF19 — `CronTrigger`, nuovo `rendicontazione/gestore_rendicontazione.py` accanto al CRUD generico già presente lì). RF19 aggrega gli ordini dei viaggi partiti quel giorno (stesso pattern giorno-per-`data_partenza_prevista` di RF13) per `Ordine.negozio_partner` — nuovo campo opzionale, non presente nel CSV/EA, popolato da `GestoreLogistica.importa_ordini()` a partire dal nome del file CSV (dettagli in `divergenze-ea.md`, punti 17-18). Genera un PDF via `fpdf2` in `report/` (cartella non versionata) e persiste `RegistroEsiti`/`ReportConsuntivo`, con guardia anti-duplicazione per data. "Invio" del report (nel titolo RF19) non implementato: nessun contatto email per negozio partner nel modello dati. 14 nuovi test in `test_gestore_logistica_rf14.py`, `test_gestore_rendicontazione_rf19.py`, `test_scheduler.py`.
- PR #5 (caricamento duplicato/rotto via web UI) chiusa senza merge.
- **RF14 (verifica partenza automatica) e RF19 (generazione report periodico), PR #13 "feat/scheduler-rf14-rf19", mergiata in `main` il 2026-07-12 (commit `e46cbf0`):** `GestoreLogistica.verifica_partenze()` (RF14) e `GestoreRendicontazione.genera_report_giornaliero()` (RF19), scheduler interno `scheduler.py` (APScheduler, `BackgroundScheduler`). Fix applicati durante la review prima del merge: `avvia_scheduler()` sollevava un `ValueError` generico e poco leggibile se `config.ini [scheduler].report_orario` era malformato — aggiunta `_parsa_orario_report()` con validazione esplicita; `verifica_partenza_intervallo_minuti` non aveva validazione (un valore `0` diventava silenziosamente un intervallo di 1 secondo) — aggiunta `_parsa_intervallo_minuti()`; N+1 query in `genera_report_giornaliero()` risolta con `.options(selectinload(Viaggio.ordini))`; bug reale trovato da `code-reviewer` — il report rifiutava per sempre di rigenerarsi per una data già processata, escludendo permanentemente un ordine ancora `IN_CONSEGNA` al momento della prima generazione anche una volta completato/fallito — ora `genera_report_giornaliero()` aggiorna la riga `ReportConsuntivo` esistente invece di rifiutare o duplicarla (test di regressione in `test_gestore_rendicontazione_rf19.py`).
- **RF15-RF18 (rendicontazione ed esiti, tranne RF19), branch `feat/rendicontazione-rf15-rf18`, 2026-07-12 — in fase di merge in `main` insieme a quanto sopra:** `rendicontazione/gestore_esiti.py` con `GestoreEsiti.registra_esito()` (RF16: causale obbligatoria se Fallito, richiede `ordine.viaggio.stato_viaggio == IN_CORSO` — non `Ordine.stato_ordine == IN_CONSEGNA`, valore mai impostato da nessun codice esistente, verificato con grep; aggiorna sia `EsitoConsegna` sia `Ordine.stato_ordine`/`viaggio_id`; `RegistroEsiti.data_riferimento` calcolato sulla `data_partenza_prevista` del viaggio, non su "oggi", per restare coerente col raggruppamento già usato da `calcola_piano`/RF13) e `ripianifica automaticamente` l'ordine Fallito (RF17: torna `RICEVUTO`, `viaggio_id=None`, dentro la stessa chiamata — nessun passo separato). `carica_prova_documentale()` (RF18) copia fisicamente il file in `allegati/<esito_id>/` (nome univoco con prefisso, non sovrascrive prove omonime; percorso sanificato con `Path.name` contro path traversal) invece di salvare solo il riferimento al percorso originale. `rendicontazione/visualizza_consegne_transito.py` con `VisualizzaConsegneInTransito` (RF15, DTO). Pianificato con `task-exec` (adattata: nessun tracker Notion) + `pre-mortem` (2 fix applicati prima di scrivere codice: `data_riferimento` sul giorno di partenza non su "oggi"; copia fisica degli allegati non solo riferimento) + `code-reviewer` post-implementazione (2 fix reali: path traversal su `nome_file` in `carica_prova_documentale`, sovrascrittura silenziosa di allegati omonimi). File separati da `rendicontazione/gestore_rendicontazione.py` (deliberato, quel file apparteneva alla PR #13, ora mergiata). Storia completa nella memoria personale `piano-rf15-rf18.md`.
- Suite pytest verde: 153 test.

## Non ancora fatto

- **RF1-RF8** (gestione risorse umane e mezzi): PR #15 aperta (`feat/manage-resources-rf1-rf8`), non ancora mergiata — `CHANGES_REQUESTED` al 2026-07-12 (cascata di disattivazione incompleta su un `Viaggio IN_COMPOSIZIONE` già aperto prima del licenziamento/dismissione).
- Multithreading (RNF3) — import CSV e motore di ottimizzazione devono girare in thread separati dalla GUI.
- GUI vera e propria (in blocco unico, quando tutto il backend sarà pronto) — include la validazione "in tempo reale" di RF11 vista in GUI, da costruire sopra `valida_ordine_per_viaggio()` già pronta.
- Riportare su EA tutte le divergenze accumulate (vedi `divergenze-ea.md`, 19 voci) e i 22 fix dell'audit (vedi `modello-ea.md`) — incluso il package `ottimizzazione/` (DBSCAN + Held-Karp), il nuovo stato `StatoViaggio.IN_COMPOSIZIONE`, e il vincolo di durata che riapre la nota "non è un problema di instradamento (VRP)".
- Aggiornare il documento requisiti di tesi con RNF4 a 3 minuti (fuori da questo repo, a carico dell'utente).
- **Rimozione di un ordine da un viaggio in composizione** (non richiesta per RF10/RF11 v1, segnalata come estensione naturale non implementata).
- **Pulizia/timeout di viaggi lasciati in stato `IN_COMPOSIZIONE`** indefinitamente (nessun meccanismo di scadenza).

## File morti / piccoli refusi da sistemare quando si torna su quella parte

- `gui/style.py` (119 righe di costanti di stile PySide6) non è importato da nessuna parte — `main_window.py` è tornato a 8 righe dopo la richiesta di rimuovere la GUI da una PR. Da tenere in attesa della GUI vera, o rimuovere — non deciso.
- `risorse/gestore_sqaudre.py` ha un refuso nel nome file (manca la "u": dovrebbe essere `gestore_squadre.py`).
- `dati_esempio/Ordini_Expert_20260706.csv` ha un header diverso da quello atteso dall'import (`RequiereSponda;RequiereCertGas` invece di `Volume`) — con il codice attuale verrebbe scartato per intero. Il nuovo `Ordini_Expert_Nazionale.csv` (PR #11) usa invece l'header corretto.

## Prossimi passi consigliati, in ordine

1. Sbloccare/mergiare PR #15 (RF1-8).
2. Multithreading (RNF3) — PR #14 aperta, anch'essa `CHANGES_REQUESTED`.
3. Riportare le divergenze accumulate (19 voci) sul modello EA, incluso il package `ottimizzazione/`, `StatoViaggio.IN_COMPOSIZIONE`, `Ordine.negozio_partner`, il modulo `scheduler.py`, il naming `GestoreEsiti`/`VisualizzaConsegneInTransito` (non modellati come classi separate nel diagramma EA, che ha solo `GestoreRendicontazione`), e la nota "non è un VRP" da aggiornare.
4. GUI, come blocco unico finale — include il collegamento della validazione RF11 (`valida_ordine_per_viaggio`, già pronta) e delle viste RF7/8/15 all'interazione "in tempo reale" prevista dai requisiti.
